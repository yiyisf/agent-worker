{
  "name": "@ca/memory",
  "version": "0.0.0",
  "description": "StateStore / BlobStore / MemoryStore 接口与实现",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "sideEffects": false,
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts"
  }
}
