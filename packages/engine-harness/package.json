{
  "name": "@ca/engine-harness",
  "version": "0.0.0",
  "description": "适配 AI SDK HarnessAgent（Claude Code / Codex / Cursor / OpenCode / Pi 等）",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts"
  },
  "dependencies": {
    "@ca/core": "workspace:*"
  },
  "peerDependencies": {
    "@ai-sdk/harness": "*"
  }
}
